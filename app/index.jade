doctype html
html(lang='en' ng-app='morphopedia' ng-controller='RootController' ng-class='{ "black-background": state.current.name === ( "root" || "root.section-state" ) && stateParams.section !== "news" && stateParams.section !== "search", "freeze-body-scroll": state.current.name === "root.section-state.project-state" }' )
	head
		base( href='/' )

		title
			| Morphopedia {{ stateParams.section ?  '- ' +  stateParams.section.charAt( 0 ).toUpperCase() + stateParams.section.slice( 1 ) : '' }}
		meta( name='viewport', content='width=device-width, height=device-height, initial-scale=1')
		link( rel='icon', type='', href='images/favicon.ico' )
		link( rel="stylesheet" href="http://cdn.leafletjs.com/leaflet-0.7.1/leaflet.css")
		link( rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/leaflet.markercluster/0.4.0/MarkerCluster.css")
		link( rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/leaflet.markercluster/0.4.0/MarkerCluster.Default.css")

		//- BaguetteBox CSS
		link( rel='stylesheet' href='https://cdn.rawgit.com/feimosi/baguetteBox.js/master/dist/baguetteBox.min.css' )

		//- BaguetteBox JS
		script( src='https://cdn.rawgit.com/feimosi/baguetteBox.js/master/dist/baguetteBox.min.js' )

		//- Image loading
		script(src='https://cdn.rawgit.com/jongacnik/mbl/master/dist/mbl.bundled.js')

		//google analytics
		script( type='text/javascript').
			(function (i, s, o, g, r, a, m) {
				i['GoogleAnalyticsObject'] = r; i[r] = i[r] || function () {
					(i[r].q = i[r].q || []).push(arguments)
				}, i[r].l = 1 * new Date(); a = s.createElement(o),
				m = s.getElementsByTagName(o)[0]; a.async = 1; a.src = g; m.parentNode.insertBefore(a, m)
			})(window, document, 'script', '//www.google-analytics.com/analytics.js', 'ga');

		// min:js 
		// endinject 
		// min:css 
		// endinject 
		// inject:css 
		// endinject 
		// inject:bower:js 
		// endinject 
		// inject:js 
		// endinject 
	body

		//- You could put other ui-views here, like a modal.

		script( src="http://cdn.leafletjs.com/leaflet-0.7.1/leaflet.js")
		script( src="https://cdnjs.cloudflare.com/ajax/libs/leaflet.markercluster/0.4.0/leaflet.markercluster.js")
		script( src='http://maps.googleapis.com/maps/api/js?v=3.13&key=&sensor=false&extension=.js')
		script( src='/images/Google.js')

		.root-view( ui-view='root' )

